FROM apache/airflow:2.8.0-python3.12

# Install NBA project dependencies so DAG workers can import etl.* and models.*
# This runs as the airflow user — no sudo needed for pip.
COPY requirements.txt /requirements.txt
RUN pip install --no-cache-dir -r /requirements.txt
