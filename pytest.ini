[pytest]
testpaths = Testing
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: requires live network access to nba_api (skip in CI with -m "not integration")
addopts = -v --tb=short
