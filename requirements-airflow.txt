# Airflow DAG dependencies
# SQLAlchemy is pinned to 1.4.x — Airflow 2.9.x is incompatible with SQLAlchemy 2.x.
# Pinning prevents transitive dependencies from upgrading it.
# mlflow / streamlit / altair / plotly excluded (not needed in DAG workers).

sqlalchemy==1.4.49
nba_api>=1.4.0
requests>=2.31.0
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0
psycopg2-binary>=2.9.0
xgboost>=2.0.0
scikit-learn>=1.3.0
shap>=0.44.0
joblib>=1.3.0
scipy>=1.11.0
python-dotenv>=1.0.0
